# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gomint_db
DB_USER=postgres
DB_PASSWORD=password

# JWT Secret
JWT_SECRET=your-secret-key-here

# Server
PORT=3001
NODE_ENV=development

# Mongo (Optional - for existing MongoDB features)
MONGO_URI=

# Seed data
SEED=false
